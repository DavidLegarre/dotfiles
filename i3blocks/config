# i3blocks configuration file
#
# The i3blocks man page describes the usage of the binary,
# and its website describes the configuration:
#
#     https://vivien.github.io/i3blocks


# Global properties
SCRIPT_DIR=/home/david/.config/i3blocks/scripts
separator=true
separator_block_width=15

[arch-linux]
command=echo ''
color=#FFFF00

[volume]
command=$SCRIPT_DIR/volume
interval=once
signal=10
#STEP=5%

[battery]
#label=:
#command=acpi | awk '{print $4}' | sed 's/,//'
command=$SCRIPT_DIR/battery
interval=10

[caps-lock]
command=xset -q | grep Caps | awk '{ print $2, $4 }'
interval=once
signal=11

[num-lock]
command=xset -q | grep "Num Lock" | awk '{ print $6, $8 }'
interval=once
signal=11

[time]
command=date '+%H:%M-%d/%m/%y'
interval=1
